# 版本与功能范围

## 当前文档基线

<div class="version-baseline-table" tabindex="0" role="region" aria-label="当前文档基线">

| 组件 | 文档版本 | 状态 | 说明 |
| --- | --- | --- | --- |
| Lite | `2.3.3` | 已发布 | 服务端、管理后台、MCP 代理、数据迁移与公共接口 |
| Lite-agent | `2.3.3.0` | 已发布 | 节点采集、远程控制、MCP 完整管理、自动更新与回程线路探测 |
| Lite-Theme | `1.1.2` | 已发布 | 默认公开大屏主题 |

</div>

手册已按上述组合的实际行为编写。具体构建标识以 Lite“关于”页面和 GitHub Release 中的七位构建码为准；排查问题时应同时提供版本号和构建码。

使用 Snapshot 时，应以对应 Release 的测试说明为补充，并在更新前备份完整 `data` 目录。

## Lite 2.3.3 与 Agent 2.3.3.0 重点变化

- [MCP 代理](/remote/mcp)：在后台连接 AI 客户端，选择允许管理的服务器与有效期，完成验证后可执行命令、使用交互终端和读写文件；支持查看操作记录、导出记录和撤销授权。
- [远程终端与文件](/remote/terminal)：远程管理界面与管理后台采用统一的 UI 风格，并优化了会话管理、文件操作及移动端使用体验。
- [仪表盘](/admin/dashboard)：新增近 30 天流量与日均用量摘要，点击每日计费流量柱形图可查看当天服务器明细，并搜索、排序。
- [Linux 脚本安装](/install/linux)：同时支持 systemd 主机和 OpenWrt / iStoreOS 的 procd 环境；符合条件的软路由直装实例也可使用后台“立即更新”。
- [服务器管理](/admin/servers)：节点列表提供“重置 Token”；地区筛选支持澳门。重置后需使用原节点重新生成的部署指令更新 Agent。
- [成本中心](/admin/billing)：流量重置、更换 IP 和一次性费用允许录入 `0` 金额；账单按服务器当前名称显示和搜索。
- [主题管理](/themes/)：默认 Lite-Theme 更新到 `1.1.2`，包含手机端显示优化；保留丢包率开关、探测延迟默认时间和带宽显示等设置。

## 从旧版本沿用的行为

- 自动发现新节点的接口和后台入口已经下线；新安装及 Docker 重建都使用后台为具体节点生成的普通部署命令。
- 远程终端、文件管理和远程执行统一受站点开关、Agent 本地开关及重新验证保护。短期授权绑定当前管理员登录会话；终端与文件管理共用授权范围，远程执行使用独立范围。
- 主题上传、更新、GitHub 安装和市场安装统一限制为 128 MiB，并加强网络下载地址检查。
- 完整备份会携带恢复加密两步验证数据所需的实例密钥，恢复前会校验数据库与密钥是否匹配。
- 管理员密码改用 Argon2id，旧密码会在成功登录后自动升级；2FA Secret 加密保存，并阻止同一验证码重复使用。
- 主题常用的 `common:getNodes` 不再返回 Agent 版本、部署状态和远程管理状态等后台字段。
- 远程执行可避免同一任务因重复投递而再次运行，并能在 Agent 异常退出后报告未知执行状态；旧自动发现安装只读取已有身份文件，不再注册新节点。

## 与上游的关系

Lite 基于 [komari-monitor/komari](https://github.com/komari-monitor/komari) 持续开发，但数据库结构、后台界面、Agent 更新源和部分公共接口兼容逻辑已经不同。

本手册统一使用以下口径：

- “与上游一致”表示当前 Lite 已按实际实现核对，所述行为可按同一方式使用。
- “上游兼容”只表示文中明确列出的版本、数据或接口可以迁移或调用，不代表镜像、数据库和后台实现完全相同。
- “Lite-agent”表示 `nuomiiiii/Lite-agent` 独立仓库的配套 Agent。当前只支持协议 2，不代表兼容上游或旧版 Agent。
- “本项目官方脚本”表示 `nuomiiiii/lite` 仓库发布的安装脚本，不是上游仓库的脚本。
- Lite 与上游确有差异时，页面会直接标注差异和适用范围，不要求读者自行判断。

不要混用两个项目的 Docker 镜像地址，也不要让两个项目同时读写同一份数据目录。从上游切换到 Lite 前必须备份；当前已适配的迁移来源见[更新与数据库迁移](/install/update)。第三方主题和外部集成应使用公共接口，不应直接依赖内部数据库结构。
